import {BGThemePicker} from "../internal_components/FloraThemePicker";

export default function Index() {
    return (
        <div style={{
            width: "100vw",
            height: "100vh",
            overflow: "hidden"
        }}>
            <BGThemePicker />
        </div>
    );
}
